# Chat VeigaGustavo

Interface Flutter (login + chat em tempo real) alinhada ao backend REST e STOMP.

## Executar

```bash
flutter pub get
flutter run
```

Pacote Dart: `auth_chat_interface` (nome técnico); a app chama-se **Chat VeigaGustavo**.
